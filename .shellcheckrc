# The project intentionally sources the library via an environment override.
# ShellCheck cannot follow this non-constant source.
exclude=SC1090,SC1091

# LM_PREFIX is used by the shared library for log prefixing (runtime),
# but ShellCheck can't see that cross-file usage.
exclude=SC2034
